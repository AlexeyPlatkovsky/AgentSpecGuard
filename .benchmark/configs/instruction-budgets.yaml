# Instruction budget thresholds (approximate token counts)
# Hard limits — budget check fails if exceeded

agents_md:
  hard_limit: 1500

skills:
  per_file_hard_limit: 750

workflows:
  per_file_hard_limit: 500

subagents:
  per_file_hard_limit: 1000
